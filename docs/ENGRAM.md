# Engram — AI persistent memory

Repository-local **product spec** and **`REQ-*`** traceability are described in [Spec-driven development](SPEC-DRIVEN-DEVELOPMENT.md).

This repository is **prepared to use Engram** with Cursor. Configuration: `.cursor/mcp.json` registers the Engram MCP server (`engram mcp`).

## Quick links

- Install: `npm install -g engram-sdk` then `engram init`
- Docs: [engram.fyi](https://www.engram.fyi/docs)
- Full Engram guide (install, usage, vault reset): see the shared Nowo template — this bundle ships the same `.cursor/mcp.json` pattern as sibling bundles.

For detailed Engram setup (npm/Homebrew, MCP tools, clearing the vault), refer to the canonical copy in any mature Nowo bundle or [Engram documentation](https://www.engram.fyi/docs).

## See also

- [SPEC-DRIVEN-DEVELOPMENT.md](SPEC-DRIVEN-DEVELOPMENT.md)
